export const goToHome = (navigation) => {
    navigation('/')
}

export const goToQuiz = (navigation) => {
    navigation('/quiz')
}

export const goToResult = (navigation) => {
    navigation('/result')
}
export const goToCena = (navigation) => {
    navigation('/cena')
}